# The test city — plan of record (owner decisions, 2026-08-30)

The district placer's first real outing, the core gameplay buildings'
debut, and the lighting front's fix — one build, three owner decisions:

1. **SCALE: 4–6 blocks around one arterial crossing.** Two arterials,
   districts legible, variety rules stressed, ~60–100 buildings from
   the 548-mesh catalogue, one honest board rig. Its own NEW MAP,
   created at the GUI by the owner (remote level creation is the
   documented crash trap).
2. **CORE BUILDINGS: BUILD NOW, LOOK-FIRST** (owner's explicit call,
   against the declare-and-wait recommendation — they want to see
   them). Strategy MARKETPLACE and REAL ESTATE OFFICE. Doctrine still
   applies in order: archetype DECLARATION first (look-soul + registry
   entry, function marked TBD-pending-economy-notes), CANON REFERENCE
   before geometry (slots 6–8 are open; the marketplace may absorb the
   wanted "stores reference" slot — owner supplies or blesses images),
   then recipes through genbuild, gated and stamped like everything.
   Function retrofits when the economy notes land; the owner accepts
   that rework risk knowingly.
3. **LIGHTING: folded into the test-city rig.** The read #2 tell
   ("render-like") gets its investigation during the new board's rig
   derivation — shadow-edge character, studio-lamp falloff/warmth,
   room-occlusion ambient, practicals' contribution (hypothesis list in
   Saved/ColdRead2/RECORD.md). Fix it on the city every future reader
   sees. Numbers gate the look; the owner's eyes decide it.

## Sequence

    1. district placer contract: design-session pushback -> owner
       blesses schema v0                       (the only City Sample
       adoption that gates the city)
    2. placer v0: roads-first, metadata-emitting, known-answer parcel
       self-test, variety rules; owner creates the map at the GUI
    3. parallel: core-building declarations + canon refs -> recipes
    4. city placed; rig derived; lighting investigation runs on it
    5. boom rig dropped in (BoardCentre measured, never authored)
    6. later, on their own gates: CityTick traffic (needs economy
       notes + placer parking points), read #3 (wide-framing SHOW
       frames included, verbatim transcript)

## Standing constraints carried forward

Bake policy governs (the catalogue is current; placement needs no
bake). Declare before geometry, even look-first. One writer in the
editor. Evidence under Saved/. The placer emits per-parcel metadata
from its first run — frontage, camera POIs, practicals, parking,
ambience, econ hooks empty until the notes land.
