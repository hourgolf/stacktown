# Polish backlog

Known, reproduced, and deliberately deferred. Everything here was seen in a
rendered image and is described by what is wrong rather than by a guess at the
cause — a backlog entry that names a cause it has not isolated becomes a false
finding, which this project has already paid for once (see `DONORS.md`).

## Recipes

| # | Recipe | What is wrong | Why it matters |
|---|---|---|---|
| P1 | `vernacular2` Loft | The **hoist gantry is too small to register**. It is one beam plus a brace at 118 uu oversail; at building scale it reads as a small box on the parapet. | It is the detail that says "warehouse" from a block away, and right now it says nothing. Wants a proper two-post gantry with a head frame. |
| ~~P2~~ | ~~`contemporary2` Mass Timber / all~~ | **DONE.** `flank()` now reads `flank_kind(spec)` — the SAME feature flags the generator reads — before falling back to style. Rather than a builder per recipe, there is one parameterised `flank_param` and a `FLANK_KINDS` table of four knobs (vertical member step, horizontal courses, how proud, opening fraction): a flank is only ever those few decisions. Ten kinds cover 14 recipes; the other 18 correctly fall through to their style's canonical flank. `flank_allowance` reads the same table, so the geometry and the allowance can no longer disagree. |
| P3 | `modern2` Precast | The **service shaft does not read** — it is the same `MI_concrete` as everything around it, so a full-height expressed shaft disappears. Binds only **6 materials**. | The shaft is the one thing stopping a brutalist block being a box. Give it the trim role. |
| ~~P4~~ | ~~`deco` Deco~~ | **EXPLAINED BY P13.** The recipe bound 6 materials and ran thin because it had no glazing — no `Glass_`, `Interior_`, `Frame_` or `Mullion_` parts existed on any floor. Parts per model went 639 -> 831 once the loop was fixed. Worth re-measuring the density across the deco ladder now before deciding whether any real plainness remains. |
| P5 | `deco2` Streamline | The **rounded end does not read** from a normal viewing angle — the stepped returns are there but edge-on. | The radius is half the style's identity. |
| P6a | `modern3` Pavilion | The **flank is `flank_modern`** — deep horizontal spandrel recesses against a front that is a fine steel-and-glass grid. Third recipe to hit this; see P2. |
| P6b | `contemporary3` Rainscreen | The **openings read as pale panels rather than glazing** — the reveal surround is light and larger than the glass, so it dominates. Wants a darker reveal or a smaller one. |
| P6c | `vernacular3` Terrace | The **canted bays read as flat projections**, not canted — the two 22 uu cheeks are too narrow to show at this scale. |
| P6d | `deco3` The Works | Binds **6 materials**; the stepped arch heads read well but the building is otherwise very plain brick. Note this one was NOT affected by the P13 loop bug — the giant order draws its own glazing — so its plainness is real. |
| ~~P16~~ | ~~gate / `modelgate`~~ | **DONE — GATE-09.** "A building with floors carries at least 1 `Glass_` part per floor." Ships with a self-test that sees its own defect (a full elevation of walls with no opening) and correctly exempts a hoarding at floors=0. Threshold set from measurement, not taste: the tightest healthy model in the catalogue sits at **1.08** parts per floor, so 1.0 fires on the defect and on nothing that works. Swept over all 194 shelf-width models: **0 false failures.** |
| ~~P13~~ | ~~`deco`, `deco6`, `deco8`~~ | **DONE.** Root cause was not ornament level at all: `build_deco`'s floor loop read `range(0 if not GO else F)` — inverted from the day the giant order was added — so **every deco recipe except the works ran zero floors and had no glazing whatsoever**. The pale stripes between the pilasters were the core showing through. Fixed to `range(0 if GO else F)`. On top of that, `deco8` is now a **banded** facade with punched openings (no vertical order at all) and `deco6` has continuous 100 uu balconies on every floor with wider bays and shallower piers. All three now read as different buildings. |
| ~~P6~~ | All | **CORRECTED, then closed by a placement rule.** The recorded cause — a shared `seed` — is wrong about the mechanism. Diffed against the generator's own recorded part list, a different seed swaps two rooftop clutter meshes (an air conditioner becomes an antenna, a water tank) and **moves no geometry at all**: 0 fields on `vernacular3` t4 and `deco` t5, 3 on the recipes that own a stairhead, none of them structural. Per-parcel seeds would not have fixed this. <br><br>What moves is the **parcel**. Measured on `vernacular3` t4: width 1640→2050 restructures **322 parts** and pushes 5 past the gate's 39 uu block threshold, up to **410 uu**; a tier step moves 243. So the fix is a placement rule and needs no generator change — `street.vary_repeats()` forbids any two parcels on the block from sharing (recipe, tier, width), which is exactly the baked asset name. Width is tried before tier, widest change first, because it restructures more and keeps the building's story (same type, bigger plot) rather than saying the owner rebuilt. |

## Pipeline

| # | Area | What is wrong |
|---|---|---|
| ~~P14~~ | ~~`shelf.py` / stage~~ | **DONE.** The shelf lays ladders out in a **4-column grid** (cell width measured from the longest ladder, not guessed) instead of 32 stacked rows: footprint went from 70,400 uu of y to 71,436 x 14,428. New `groundfit.py` then sizes STAGE_Ground to the shelf's own reported bounds, so the bench can never again be built off its floor. The whole catalogue reads in a single frame. |
| P7 | `shelf.py` | Rows are ordered alphabetically and shot from one camera, so a row is almost always occluded by its neighbours. Every row capture this session needed hand-placed cameras and several of them still framed the wrong row. Wants a per-row camera helper that shoots each ladder from its own clear side. **Partly eased by P14** — the grid separates the columns, so an overview now works; a single ladder still has no camera of its own. |
| P8 | `preview.py` | Runs `fastbake.py` with `capture_output=True` and surfaces only the `FASTBAKED` line, so debug prints inside the bake vanish. Cost hours on the lawn bug. Either pass through lines matching a marker, or document the `./Tools/rung.sh fastbake.py` route in the file. |
| P15 | bake scripts | The batch scripts filter output through `grep -E "gate FAIL\|GATE-\|STAMPED"`, so a **Python traceback or a skipped job is invisible** — a run reports "all passed" while silently building nothing. The 194-mesh bake came back 188/194 with zero failures because a job list written without a trailing newline made bash's `while read` drop its last line (`vernacular8`, sorted last). Batch runs must **reconcile expected against stamped**, not just grep for failures. Same class of fault as P8. |
| ~~P9~~ | ~~`cores.py`~~ | **DONE, but the item was wrong.** These three do not need `open_roof` and never did: `build_house`, `build_walkup` and `build_works` each build one solid `Wall_Body` with four real walls — they are buildings, not facade shells, which is why `step_elevations` already excluded them from flanks. The actual defect was that **`cores` did not know that**, so adding a recipe for any of them would have put a redundant solid core inside an already-solid building *and* buried its roof. `cores.DETACHED` / `cores.is_detached()` now exist, `bands_for` returns no bands for them, and `step_elevations` imports the list instead of keeping its own two copies. <br><br>Also worth recording: **`walkup` is retired** (recipes.py says so explicitly) and none of the three has a recipe — they are reached only by the city block builders in `city.py`. So this was pre-emptive, not a live defect in the catalogue. |
| ~~P12~~ | ~~`cores.py`~~ | **DONE.** New `cores.setback_at(spec, f, F)` is THE resolver, with `setback_mode` of `progressive` (default) or `constant`. The formula had been written out **four separate times** — in `cores.bands_for`, twice in `build_modern`, and in `build_contemporary` — which is exactly how a floor steps back and its core does not. All four now call the one function, and `cores.setback_top` gives the roof its offset. `modern6` is a real **podium** again: one constant step, not a stair. |
| P10 | `genbuild.py` | `CONT_LOGGIA_D` and `CONT_BALC` are now unused constants left from the pre-canon contemporary facade. Dead. |

## Stage 2 gate — first street test, 2026-08-27

| # | Finding |
|---|---|
| S1 | **Player zoom PASSES.** A street-level shot down the canyon reads as fabricated card: fluted pilasters casting real shadow, paper tooth visible, deep window reveals, cill bands. The Stage 1 surface toolkit transfers to a street unchanged. |
| S2 | **Block hero is WEAK, exactly as measured.** Every building is a flat-fronted rectangular prism standing on one continuous building line. What differentiates them at range is COLOUR and HEIGHT — not architecture. This is the F1 reader's finding reproduced: 13–31 depth planes at zoom, only **3–5** that survive the gate's own 39 uu block threshold. |
| ~~S3~~ | **ADDRESSED.** `street.py` now varies the building line per parcel (0–210 uu setback, some flush) and the gap to its neighbour (40–300 uu), both deterministic per parcel and both well above the gate's 39 uu threshold. Broke the comb, though on its own the gain was modest. |
| ~~S4~~ | **ADDRESSED, and the cause was mine.** The first street used almost no recipe that carries a crown, at tiers where the crowns do not appear — so every silhouette was a flat parapet. That was a **composition failure, not a catalogue one**. The catalogue holds ziggurats, masts, stacks, blade signs, cupolas, gables, pediments, chimney stacks and mech penthouses, all above the block threshold; the mix simply never asked for them. Rebuilt so no two neighbours share a crown TYPE and the silhouette varies properly. <br><br>**The lesson generalises:** the block-hero read depends as much on WHICH buildings get placed next to each other as on what the buildings are. That is a placement-rule problem for `BP_Parcel`, and it needs stating as a rule — a district that picks recipes at random will reproduce the comb. |
| ~~S5~~ | **DONE — `blockrig.py`.** A key/fill rig sized and aimed for a block, DERIVED not guessed, per the gate: intensity scaled by (new rig distance / old)² from the measured board rig (key 950,000 lm at 5,595 uu; fill 520,000 at 4,301), emitters scaled linearly by the same ratio because a softbox that lights one building edge-to-edge is a hard little source at fifteen thousand. At a 14,000 uu rig: **key 5,948,117 lm, fill 5,509,606 lm**, emitters 6506×4254 and 13020×8463. Both MOVABLE so they stay out of the stationary overlap budget. Additive — the board rig is untouched. <br><br>Canyon interior went **13.4 → 35.8 mean, 0.000% clipped**; block hero 92.8 → 105.9 with sd up and still no clipping. <br><br>**Trade-off, not a clean win:** the first attenuation radius (RD×2.6 = 36,400) reached the shelf's near corner 21,000 uu away — a block rig quietly relighting the review bench makes later captures incomparable. Tightened to RD + the subject's half-diagonal (22,500), which costs the street's far end some light (canyon 35.8 → 28.6). **The real fix is the one the Stage 2 carve-out already specifies — street and bench in separate maps.** Blocked on map creation: `load_level` over remote execution crashes the editor, so the owner has to make and open it. |
| S11 | **CRITICAL — `piece()` places NO donor geometry in live mode, and has been silent about it.** `genbuild.piece()`'s live branch calls `ue.tool(S, 'add_static_mesh', ...)` and discarded the return. `describe_toolset` confirms **`SceneTools` has no `add_static_mesh`** — `box()` uses `PrimitiveTools`, `piece()` points at the wrong toolset. With the response now parsed, a known-good AssetsvilleTown asset fails: `TOOL-ERROR: Unknown tool add_static_mesh`. **Every donor fails, not just Uniblocks.** <br><br>Consequence: models built through the LIVE path (`bake_catalogue.py`) contain **no roof planting, tanks, AC units, drainpipes or flowerbeds** — and were stamped `Gate=PASS` anyway. Models built through the FASTBAKE path (`preview.py`, records → merged geometry) do carry them, which is why `SM_Bld_vernacular5_t5_w1640` has `MI_planter`/`MI_gravel`/`MI_leaf_card` and today's `bake_catalogue` output does not. <br><br>**An unknown subset of the 297 baked meshes is missing all of its donor geometry, and nothing on the asset says which** — stamps record recipe/tier/width/gate but not which path baked it. This is precisely the path-provenance gap the 27 Aug code review named, arriving as a concrete defect rather than a theoretical one. <br><br>Fixed so far: `piece()` now parses the response, prints each failure with its asset path, and collects them in `genbuild.piece_failures()` so a baker can REFUSE rather than stamp a model missing parts it thinks it has. **FIXED 27 Aug and verified.** `add_static_mesh` exists on **no toolset at all** — it never has. The working sequence is a composite of two tools that do exist: `ActorTools.add_component` (StaticMeshComponent) then `ObjectTools.set_properties`. The encoding was probed, not guessed, and one part of it would certainly have been got wrong: **`StaticMesh` takes the DOTTED object path** (`/Game/x/SM_y.SM_y`); a bare path string and an object-shaped `{refPath:...}` are both rejected. Transform structs accept either case. <br><br>Verified by **bounds growth, not by a returnValue** — placing the Assetsville tank moved the actor's z-top from 128 to 864.7 against the tank's measured height of 876.5 in `meshbounds.json`, which proves the mesh resolved and rendered bounds rather than that a property string was accepted. <br><br>**Still outstanding: no re-bake.** Every mesh baked through the live path before this fix is still missing its donors, and nothing on the asset says which those are. Needs path provenance on stamps plus the donor-materials reconciliation. |
| S12 | **The canopy bug: a LOOP VARIABLE SHADOWING A FUNCTION PARAMETER.** `build_vernacular(spec, origin, yaw)` took `yaw` as a parameter; the flowerbed loop was written `for mesh, (mx, my, mz), yaw in ubkit.bed(...)`. Python leaks a loop variable into the enclosing scope, so after the last bed piece `yaw` held ITS rotation for the remainder of the builder. The only `mkactor` after that point is the canopy — so the canopy alone was created at yaw 180 and flipped whole about the actor origin, putting local x 68..width at world x −(width−68)..−68, entirely off the parcel. <br><br>**The number named the culprit.** The ladder sweep's five failures read 752 / 1162 / 1572 uu over on parcels of 820 / 1230 / 1640 — width minus 68, every time. It fired only on the two recipes carrying BOTH a flowerbed and a canopy (`vernacular` t4, `vernacular5` t5). GATE-05 was right every time it refused them. <br><br>Fixed by renaming the loop variable to `bed_yaw`. Known-answer check: all five combinations went from 752–1572 uu over to **0.0, FITS**. <br><br>**Fourth frame-confusion defect in this project** — after `Rotator(roll, pitch, yaw)` order, GATE-10 world-vs-local, and the sweep's unapplied actor rotation. When geometry lies here, check the frame before the geometry. |
| S13 | **Fixing S12 broke the sweep's own known-answer check, and that is worth recording.** The check's fail-case was a REAL broken combination (`vernacular5` t5 w1230 at 1162 uu). The canopy fix repaired it, so the check silently ended up with two passing cases and no proof it could detect anything — an instrument calibrated against a bug stops being calibrated the moment somebody fixes the bug. <br><br>Replaced with a SYNTHETIC planted defect per `INVARIANTS.md`: build a model that fits, yaw one of its actors 180, require the sweep to see it. Reads clean 0.0 / planted **1568.0 uu DEFECT SEEN**. Same fault class as the bug it replaced, so the check keeps its provenance without depending on the bug surviving. |
| S14 | **The hand-tolerance jitter called the editor from RECORD mode — 7 unguarded sites.** `build_vernacular` (5), `build_modern` (1) and `build_deco` (1) called `ObjectTools.set_properties` directly to set each floor a percent or two off square. Unlike every other emitter in `genbuild`, these had no `_SINK` branch, so they fired during offline builds too. <br><br>Three consequences, in increasing order of how long they hid: (1) in record mode `instance` is the sink's integer ref, not an actor path, so the call could only fail — and its return was discarded, so it failed **silently**, exactly like `piece()`; (2) an offline sweep that should never touch the editor opened an MCP socket and made a blocking round trip **per floor**, which with a busy editor sits on a 180s timeout each — that is what stalled the ladder sweep at low CPU with an ESTABLISHED connection to port 8000; (3) the jitter is applied to the LEVEL and never to the recorded parts, so **live-baked meshes carry hand tolerance and fastbaked ones do not**. <br><br>**Measured effect of the guard: the full 548-combination sweep went from 1635s to 1s, with zero sockets.** The sweep was never compute-bound; it was ~99.9% blocking HTTP. <br><br>(3) is left OPEN deliberately — recording the jitter and having fastbake apply it changes bake output, so it is separate work, not a drive-by. It is the same live-vs-fastbake divergence family as S11. |
| S15 | **A ZERO-DIFF IS NOT EVIDENCE UNTIL THE DIFF HAS PROVEN IT CAN SEE A DIFFERENCE.** Verifying the donor renames repainted nothing, I produced a confident `COMBOS WITH ANY DIFF: 0` that was **void** — it compared a dataset to itself. Two harmless-looking things composed: I ran a colleague's script with a `--out` flag it does not have (assuming a tool's interface instead of reading it, which also overwrote their frozen baseline), and their script hardcodes `sys.path.insert(0, <live Content/Python>)`, which silently defeated the `PYTHONPATH` I used to point at a reverted copy. So the "before" run imported the renamed code. The false zero read *exactly* like the real one. <br><br>Caught by a **precondition**, not by inspection: "BEFORE records already roled: 3831". The fix is to make the comparator refuse to render a verdict until it has shown discriminating power — for this diff that means asserting the two input hashes DIFFER, that BEFORE has zero roled names and AFTER has zero unroled, before any comparison is reported. <br><br>Same family as S13 (an instrument calibrated against a real defect fails open once the defect is fixed). Both are the general rule: **a check that cannot demonstrate it would fail is not evidence, however confident its output.** |
| S16 | **Leaf cards render dark in BAKED meshes; the fault is in the MERGE, not the bindings.** Localised by measurement, not inspection: (1) `rolemap.material_for_slot` resolves the donor slots correctly — `testleaf_01 -> MI_leaf_card`, `testtrunk_01 -> MI_wood`; (2) `MI_leaf_card` itself is correct — parent `M_StacktownMaster_Masked`, `BaseColour (0.300, 0.420, 0.215)` green, `LeafMask = T_leaf_01a` set; (3) after a live build the components are bound correctly — `Leaf_Tree0 -> ['MI_wood', 'MI_leaf_card']`, `Leaf_Tree1 -> ['MI_wood', 'MI_leaf_card_b']`, and `step_roles` reports `assigned 245 slots; unresolved []`. <br><br>**But the merged asset `SM_Bld_vernacular_t4_w820` has 16 material slots and NO `MI_leaf_card` among them** — while the generator emits 12 leaf-card pieces including 3 trees for that exact combination. So `SceneTools.merge_actors` is losing the leaf slot. Everything upstream of the merge is verified correct. <br><br>**ANSWERED by triangle count: the merge drops the SLOT, not the geometry.** Baked `vernacular t4 w820` carries **51,274 triangles** against a donor budget of 27,258 — the tree/bush geometry (15,136 tris) is all present. But 19 distinct materials are expected pre-merge and only **16 slots** survive, and the ones missing are exactly **`MI_leaf_card` and `MI_leaf_card_b`** — the two instances of the MASKED master. So the leaf cards survive as geometry and get rebound onto an opaque material, which is precisely the dark-quad read on screen. <br><br>**`SceneTools.merge_actors` cannot carry a masked-blend-mode material through.** The MCP tool exposes only actors/output_path/name/destroy_source_actors, so there is no merge setting to reach for. <br><br>**THE CONTROL THAT SETTLES IT: fastbaked meshes DO carry the leaf materials.** `SM_Bld_vernacular5_t5_w1640` (fastbake, stamped 26 Aug, pre-provenance) has both `MI_leaf_card` and `MI_leaf_card_b` in its slots. Same generator, same donors, different bake path — and only the live path loses them. <br><br>**RESOLVED by baking through fastbake — verified visually 27 Aug:** the fastbaked mesh renders its trees as green foliage with leaf shapes and brown trunks, where the live-merged mesh from the same camera showed dark grey smudges. Frame means 158.56 vs 156.23, so the lighting is comparable and the difference is the foliage itself. <br><br>**Consequence for the wave, and it is good news:** the fastbake path fixes the leaves AND is ~60x faster (measured live rate 0.70 s/part = 30.4 h for the remaining 258 meshes). Fixing S14 so fastbake carries hand tolerance therefore resolves S16 and the wave duration in one move, rather than being two separate jobs. |
| ~~S14~~ | **DONE — hand tolerance is now RECORDED, so fastbake carries it.** The jitter is folded into the ACTOR's recorded transform, which fastbake already composes each part's world transform from — so it needed no change at its end and the live path is untouched. Verified: floor actors now carry sub-degree rotations and offsets up to ~1.7% of width, exactly the "model tolerances, 1-2%" the code always intended. <br><br>Regression-checked against the ladder: **548/548 still fit their parcels, 0 oversail**, with the planted defect still detected at 1568 uu. The jitter has headroom because the building is inset by the flank allowance. |
| S17 | **RE-BAKING BREAKS EXISTING ACTOR REFERENCES.** Fastbaking `SM_Bld_vernacular_t4_w820` left the staged `PILOT_t4` actor with `static_mesh = NONE` and a zero-extent bounds — it renders nothing, silently. Found only because a capture came back as empty board and I checked the actor instead of assuming the mesh was wrong. <br><br>**Wave consequence:** the 19 `ST_` actors in `Stage2_Street` all point at baked assets. A re-bake wave will null every one of them. `street.py` must be re-run after the wave, and any capture taken before that re-run is of a partially empty street. Cheap to handle, expensive to discover from a confusing image. |
| S18 | **My ladder sweep measured ONE AXIS. GATE-05 checks two.** The sweep reports X oversail against the parcel line and never looked at depth — so "548/548 buildable" was a WIDTH claim reported as if it were a GATE-05 claim. The wave found the gap the honest way: 18 refusals, every one `contemporary6`, all six tiers x three widths, all `GATE-05 depth 1107 exceeds 900 + 130 oversail`. <br><br>And the depth overrun is DONOR-DRIVEN, which a box-only check could never see: boxes alone span **961** (inside the 1030 allowance), boxes plus donors span **1107** — matching the gate's number exactly. Same donor-blindness as S16/S11, on the axis I never extended the fix to. <br><br>Two things follow. The sweep needs its Y axis before its number means what people have been told it means. And `contemporary6` needs a real fix — it is 77 uu too deep with its donors, so retrying it in a later wave just reproduces the refusal. |
| S19 | **The editor crashed at ~475/548 of the wave. Cause unknown — recorded as unknown.** No corruption: 507 baked assets on disk, zero empty, none undersized, newest three all full-size. The wave driver stops cleanly against a dead bridge (connection refused) rather than writing garbage. <br><br>What I do NOT know: why it crashed. It was ~1750s and ~475 fastbake+stamp cycles in, on recipes unrelated to the 18 failures (those were `contemporary6`, alphabetically much earlier). **CAUSE FOUND, and it is neither of the guesses.** Both crashes are **MetalRHI (GPU) faults**, read from `~/Library/Logs/Unreal Engine/StacktownAlphaEditor/`: <br>&nbsp;&nbsp;20:12:42 — `Assertion failed: RenderPass [MetalCommandList...]` <br>&nbsp;&nbsp;20:56:57 — `Fatal error: [MetalCommandList.cpp:90]`, stack `ReportMetalCommandBufferFailure -> ProcessInterruptQueue -> FMetalThread::Run` <br><br>Not Python, not the bake scripts, and **not memory** — the "OOM" string I first grepped was a false positive on unrelated font/memory lines; there is no out-of-memory record in either log. Resource exhaustion is therefore FALSIFIED as the cause. <br><br>**Not a 5.8.2 regression either:** the first crash happened BEFORE the update, the second after. Same subsystem both times. This is a persistent Metal/GPU instability on this machine, independent of the version bump. <br><br>Mitigation is what already exists rather than anything new: the wave driver is **resumable** and stops cleanly against a dead bridge, and the stamps make the resume set exactly computable — after crash 1 the exact remaining 63 were derived from `BakePath` and completed in 153s. Worth trying the viewport non-realtime for long batches, since the fault is in the render thread and the bake needs no viewport. |
| S20 | **GATE-05 MEASURES IN ACTOR-LOCAL SPACE and never applies the actor's own transform.** Isolated by stripping actor loc/rot and re-measuring: `deco6` t3 w1640 spans **867 uu in the frame the gate sees** (fits its 870 allowance) and **887 in world** (over). The 20 uu difference is entirely per-actor transforms. So the gate judges a model the merged mesh will not match. <br><br>**Fifth frame-confusion defect** in this project, after `Rotator` argument order, GATE-10 world-vs-local, my sweep's unapplied actor rotation, and the canopy's leaked loop variable. The pattern holds: when geometry lies here, check the frame before the geometry. <br><br>Not fixed by me — `modelgate` is the coordinator's file and they asked me not to touch it. Reported, not repaired. |
| S21 | **My S14 fix introduced 30 marginal depth overruns, and I caused them.** Making the hand tolerance real in the baked mesh meant a floor's yaw now rotates the actual geometry — and a rotation grows the footprint in depth by about `W·sin(yaw)`, which at W=2460 costs ~21 uu for half a degree. 30 models that previously fitted went over: deco4 10, deco6 18, tower 2, worst 22.4 uu. GATE-05 could not catch them because of S20. <br><br>Fixed by bounding the yaw by its PROJECTED cost rather than a flat number — `jit_yaw()` clamps so `W·sin(yaw) <= JITTER_DEPTH_BUDGET`. Wide buildings rotate less, which is also what a hand does: absolute misalignment does not grow with the size of the piece. <br><br>Budget is **2.0 uu, not 6.0**: `deco6` spans 867 against an allowed 870 — it oversails 127 of its permitted 130 BY DESIGN — so a 6 uu budget exceeded the tightest margin in the catalogue. Two sites needed patching, not one: `build_deco` precomputes its yaw into a tuple rather than inline, and missing it left deco4/deco6 untouched by the first pass. <br><br>Result: **530 of 548 pass BOTH axes**, only `contemporary6` outstanding. Hand tolerance still applied and still visible. <br><br>**Consequence: the 530 baked meshes are stale** — they carry the old 6 uu jitter. 30 of them are genuinely over their depth allowance; all 530 differ slightly from what the generator now produces. |
| ~~S22~~ | **contemporary6's depth defect: a DONOR SCALED ON THE WRONG AXIS. Fixed; catalogue now 548/548.** The overrun was never the building. `gsc = 58.0 / size('grass_tuft')[2]` scales by index [2] = HEIGHT, which is only safe for a tall narrow piece. `SM_grassVerticalSingle` is the opposite — a wide flat ground-cover card, **498 x 479 x 122** — so at 58 uu tall it left a **236 x 227 footprint**, and the random yaw swung its diagonal to **328 uu**. The planter bed it sits in is **22 uu deep**. One tuft pushed the model 77 uu past its parcel and cost all 18 combinations at the gate. <br><br>The mesh was never wrong; the axis chosen to scale it by was. Replaced with `plant_s` (39 x 39 x 101), which at the same 58 uu height occupies 22 x 22 — the bed exactly — and added `fit_scale()`, which meets a height target without exceeding a plan budget, so the class cannot recur silently. <br><br>**Result: 548 of 548 pass BOTH axes; 548 baked, all `BakePath=fastbake`, all `Gate=PASS`, 0 `DonorFails`, 0 broken street references.** The honest-catalogue milestone. |
| S9 | **The donor bounds table exists — `meshbounds.py` → `Tools/measure/meshbounds.json`.** 107 donors measured in LOCAL space (pivot at origin), so both extent and **pivot offset** are recorded — the number `avkit` never carried. It self-checks against a bound measured by hand during the buried-shoe bug: `SM_drainPipe_ending` spans local z −59.8..0.0, and the table reproduces it exactly. It sweeps any FOLDER a kit module points at, not just `avkit.PIECES`, because the first version missed the whole Uniblocks flowerbed pack that `ubkit` places. <br><br>**`preview.py`'s `as_snapshot` is fixed** — the live, stamping gate path that dropped `kind=='mesh'` outright now transforms each donor's measured bounds by its scale and rotation. Verified: `vernacular5` t3 went from 0 to 14 donor components visible to the gate, changing measured span by 3.5 uu against a GATE-05 tolerance of 8. A donor with no bounds entry now PRINTS and is counted in `donor_blind` rather than being silently omitted — silent omission is what let this path certify a mesh the editor gate refuses. |
| S10 | **FALSIFIED: the 1162 uu oversail is NOT a donor pivot.** That was my hypothesis and it was wrong. With the bounds table complete and zero blind donors, the offline sweep still reports 0.0 uu on `vernacular5` t5 w1230, which the editor gate refuses at 1162. So the offline sweep **stays quarantined** — it reproduces the passing known answer and not the failing one, and by this project's own rule that means it reports nothing. <br><br>The cause is still unidentified and it is now more interesting, not less: something about the live build differs from the same three functions run through the recording sink. Next step is to reproduce it under `bake_catalogue` with a component-level diagnostic — **not** through `rung.sh`. I tried that and hit the deadlock documented in `CATALOGUE_PIPELINE.md` §2: `genbuild.build()` in live mode calls `ue.tool`, and an MCP call from inside a rung script waits on the thread it is running on. My own rule, written this session, and I walked into it anyway. |
| S8 | **The block rig's attenuation edge is visible on the ground.** Confirmed by eye in `st_hero6.png`: the tightened radius (S5) draws a circular falloff on the board, and the same frame catches the review shelf behind the street with the backdrop not covering the left of frame. Both are consequences of street and bench sharing `Sandbox_Bench`. Do not paper over it by widening the radius — that relights the bench. Wants the separate map. |
| S7 | **The catalogue is baked barely one width deep.** Recipes declare which of the shared S/M/L/XL ladder they accept, and across all 32 recipes that is **548 (recipe × tier × width) combinations — only 284 are on disk, 264 missing.** By width: w820 6/12, w1230 50/140, w1640 140/182, w2050 81/158, w2460 7/56. So the width axis exists in `recipes.py` and mostly does not exist in `Content/Stacktown/Baked`. <br><br>This is what made the new placement rule fall back: both repeats wanted a different width, neither was baked, and both had to take a tier step instead — the weaker lever. It also means any future district placer has a far narrower palette than the catalogue advertises. Bounded fix: bake by width rather than by recipe. <br><br>**Partly answered 27 Aug.** Two gap-fills were attempted. `vernacular3` at w2050 **baked all six tiers cleanly** — so the gaps are NOT uniformly gate refusals, and some are simply unbaked. `vernacular5` at w1230 was **refused at all six tiers**, GATE-05 reporting a 1162 uu oversail at t5. Catalogue is now **297**. <br><br>An offline GATE-05 sweep was built to size the rest without the editor, and **it does not work**: mirroring `bake_catalogue`'s spec construction it reproduces the PASSING known answer but reports 0.0 uu on the known 1162 uu failure, because it skips `mesh` records — donor pieces carry donor-local pivots and their real bounds are not in the recorded part list. A 1162 uu oversail is the signature of exactly such a pivot. Per this project's own rule a check that cannot see a known defect reports nothing, so **no number from that sweep is quoted anywhere**. To make it work it needs a mesh-bounds table read once from disk. |
| ~~S6~~ | **DONE, and it corrected P6 on the way.** The two deliberate repeats were exact copies apart from paint — but not for the recorded reason. See P6 above: the seed is not a variation lever, the parcel is. Closed by `street.vary_repeats()`. |

## Open, not root-caused

| # | What |
|---|---|
| P11 | **Two separate faults, one of them mine, plus a measurement lesson.** <br><br>**(a) Z-FIGHTING — the thing the owner was actually seeing.** Capping the core at `ztop` so roofs would be visible put its top face exactly level with every flat roof's deck top, parapet base and top-floor wall boxes. Three coplanar surfaces per building; the depth buffer had nothing to choose between them, and jagged white patches crawled across every roof. **This is a geometry fault and no amount of GI tuning can fix one** - which is why weeks of lighting work never touched it. Fixed by `cores.ROOF_CLEAR = 17.5`; the half unit is deliberate, since all hand-authored geometry here uses whole numbers and any whole-number clearance can collide by arithmetic accident (14.0 landed on `Band_StackCap`). Verified: 0 coplanar faces across the catalogue, and now gated by **GATE-10**. <br><br>**(b) A LUMEN DRIFT, and I had been making it worse.** Four Lumen cvars were left at 0 from my own diagnostic sweep and never restored - console cvars are session state and appear in no asset and no diff. Restoring them took empty-view drift 1.143% -> 0.222%. `lumen_defaults.py` exists for this now. <br><br>**(c) MY MEASUREMENTS WERE WRONG THREE WAYS.** I measured frame MEAN, which cannot see shimmer at all (half the pixels rise while half fall) - `shimmer.py` measures per-pixel delta instead. I pointed every camera at facades and empty floor while the owner's screenshots were of ROOFS. And I read 19.39% at the roof view immediately after replacing 194 meshes, reported it as a twentyfold finding, and built a four-config sweep on it - it was Lumen re-converging after a full rebake. Settled, the same view reads **0.76%, four runs within 0.01%**. A measurement taken while the scene is rebuilding is not a measurement. <br><br>**Status:** steady state is ~0.7-0.8% moving pixels everywhere, with the z-fighting gone. Whether that residual is visible to the owner is for the owner to say - my capture path may still not reproduce the live viewport. |

## Queued by the owner (via coordinator, 2026-08-27) — staleness detection

Stamp each baked mesh with a HASH of the recipe/generator content that built
it, so after any generator edit `catalogue_audit` can list which meshes are
STALE — "what needs re-baking?" becomes a measured answer instead of a
remembered one. Natural home: stamp.py writes it, catalogue_audit compares
it against the current source. Sequence after the wave lands; S-number it
on intake.
