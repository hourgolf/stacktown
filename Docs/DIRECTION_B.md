# Direction B — the simple game's reference board

**Governance (owner-ruled, 2026-08-31): this board is DIRECTION B's
comparison set and is NEVER CITABLE FOR FLAGSHIP WORK.** It mirrors
CANON.md's structure and closed-list discipline, but the two boards are
as separate as the two catalogues: a flagship judgment that cites a
direction-B image is wrong by construction, and vice versa. CANON.md
(8 slots, flagship miniature) is untouched by anything here.

## How references land

The owner drops images into `Docs/direction_b/` and the coordinator
runs the same intake as canon (one image at a time, a short quiz: what
this shows, what it locks, what it deliberately leaves open), then the
owner blesses slots. Slots are closed-list; the owner supplies or
blesses everything.

## Slots

    B1  (empty — awaiting the owner's first examples)
    B2  (empty)
    B3  (empty)
    B4  (empty)

## What this board governs, once filled

The simple catalogue's look: massing language, palette, material
flatness, what "good" means for direction B's own archetypes.py entries
(which it gets, with reasons — the flagship gate is not forced, per
BETA_TWIN_PLAN seam 3).
